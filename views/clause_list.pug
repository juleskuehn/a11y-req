extends layout

block content
  h1#wb-cont(property="name")= title
  ul
    each item in item_list
      li 
        a(href=item.url) #{item.number} #{item.name}

    else
      li There are no clauses to display.

  a(href='/edit/clause/create') Create clause