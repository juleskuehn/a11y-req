if item_list.length > 0
  table.table.table-bordered
    caption.wb-inv French caption
    thead
      tr.active
        th.col-sm-6(scope='col') Clauses de la norme EN 301 549 2018
        th.col-sm-4(scope='col') Détermination de la conformité
        th(scope='col') Clause respectée (O/N ou S.O.)
        th.col-sm-4(scope='col') Explication
    tbody
      each item in item_list
        tr
          td
            strong #{item.number} #{item.frName}
            | !{item.frDescription}
          td
            strong C.#{item.number} #{item.frName}
            | !{item.frCompliance}
          td 
          td
else
  p
    strong There are no French functional accessibility requirements. See Part A.